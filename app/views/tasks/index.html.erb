<!--サンプルページのHTMLコピー-->
<!--これからerbを利用して編集する-->
<!--新規作成ボタン編集済み　リンク先指定済み-->
<!--タスクの削除ボタン作成　リンクは未実装-->
<!--タスク名をリンクにした。　リンク先は未実装　作成日の表示はこれから修正-->
<% provide(:title, current_user.name) %>
<h1><%= current_user.name %>のタスク一覧</h1>

<div class="row">
  <div class="col-md-10 1-1">
    <%= link_to "タスク新規作成", tasks_new_path, class: "btn btn-lg btn-primary pull-right" %>
    <table class="table table-hover" id="table-tasks">
      <thead>
        <tr>
          <th>名称</th>
          <th>作成日</th>
          <th></th>
        </tr>
      </thead>

      <tbody>
          <% @tasks.each do |task| %>
          <tr>
            <td><%= link_to task.task_name, task_path(task.id), class: "task-name" %></td>
            <td><%= task.created_at %></td>
            <td>
              <%= link_to "編集", "#", class: "btn btn-success" %>
              <%= link_to "削除", "#", class: "btn btn-danger" %>
            </td>
          </tr>
          <% end %>
      </tbody>
    </table>
  </div>    
</div>